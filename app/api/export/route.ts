import {identity} from '../../lib/server';import {exportStatistics} from '../../lib/excel';import {rateLimit} from '../../lib/auth';
export async function GET(r:Request){try{const {s,m}=await identity();await rateLimit('export:'+m.id,15);const p=new URL(r.url).searchParams;const output=exportStatistics(s,m,p.get('branch')||'all',p.get('from')||'',p.get('to')||'');return new Response(output.bytes as BodyInit,{headers:{'Content-Type':output.type,'Content-Disposition':`attachment; filename*=UTF-8''${encodeURIComponent(output.name)}`,'Cache-Control':'private, no-store'}});}catch(e){return Response.json({error:(e as Error).message},{status:400});}}
